<%- model_class = CentroUsuario -%>
<div class="page-header">
  <h1><%=t '.title', :default => CentroUsuario %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:rol) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @centro_usuarios.each do |centro_usuario| %>
      <tr>
        <td><%= centro_usuario.id %></td>
        <td><%= centro_usuario.rol %></td>
        <td><%=l centro_usuario.created_at, format: :long %></td>
        <td>
          <%= link_to centro_usuario_path(centro_usuario), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to centro_usuario_path(centro_usuario), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_centro_usuario_path,
            :class => 'btn btn-primary' %>

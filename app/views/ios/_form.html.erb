<%= simple_form_for(@io) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :device_token %>
    <%= f.association :dispositivo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
